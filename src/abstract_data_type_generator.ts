"""
Abstract Data Type Generator with LaTeX Support.
Generates abstract data types that satisfy strict security constraints (e.g., immutability).
Uses dynamic typing to handle complex constraints without external runtime hooks or custom logic in __init__.py.
This module handles the core parsing, normalization, and validation of source files for this type generator.

Author: ORACLE OF THE REPOSITORY
"""

import os
from pathlib import Path
import subprocess


def main():— no markdown fences, no commentary, no explanation.
