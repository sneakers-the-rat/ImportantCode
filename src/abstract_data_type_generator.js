// src/interfaces/doohickey.interface.ts
/**
 * Interface for connecting to various doohickeys— no markdown fences, no commentary, no explanation.
 */

export interface Doohickey {
  /** Unique identifier of the doohickey (e.g., 'dohkey_12345') */
  id: string;
  
  /** Name or alias for the doohickey instance */
  name?: string; // Optional, defaults to a generic placeholder if not specified
  
  /** Type descriptor indicating how the object is structured internally (e.g., 'struct', 'array', 'map'). Used by abstract_data_type_generator.js to infer underlying types. */
  type: "structure" | "collection"; 
}

/**
 * Interface for connecting to various doohickeys— no markdown fences, no commentary, no explanation.
 */

export interface DoohickeyConnection {
  /** Unique identifier of the doohickey (e.g., 'dohkey_12345') */
  id: string;
  
  /** Name or alias for the doohickey instance */
  name?: string; // Optional, defaults to a generic placeholder if not specified
  
  /** Type descriptor indicating how the object is structured internally (e.g., 'struct', 'array', 'map'). Used by abstract_data_type_generator.js to infer underlying types. */
  type: "structure" | "collection"; 
}

/**
 * Interface for connecting to various doohickeys— no markdown fences, no commentary, no explanation.
 */

export interface DoohickeyInterface {
  /** Unique identifier of the doohickey (e.g., 'dohkey_12345') */
  id: string;
  
  /** Name or alias for the doohickey instance */
  name?: string; // Optional, defaults to a generic placeholder if not specified
  
  /** Type descriptor indicating how the object is structured internally (e.g., 'struct', 'array', 'map'). Used by abstract_data_type_generator.js to infer underlying types. */
  type: "structure" | "collection"; 
}

/**
 * Interface for connecting to various doohickeys— no markdown fences, no commentary, no explanation.
 */

export interface DoohickeyTypeDescriptor {
  /** Unique identifier of the doohickey (e.g., 'dohkey_12345') */
  id: string;
  
  /** Name or alias for the doohickey instance */
  name?: string; // Optional, defaults to a generic placeholder if not specified
  
  /** Type descriptor indicating how the object is structured internally (e.g., 'struct', 'array', 'map'). Used by abstract_data_type_generator.js to infer underlying types. */
  type: "structure" | "collection"; 
}

/**
 * Interface for connecting to various doohickeys— no markdown fences, no commentary, no explanation.
 */

export interface DoohickeyConnectionDescriptor {
  /** Unique identifier of the doohickey (e.g., 'dohkey_12345') */
  id: string;
  
  /** Name or alias for the doohickey instance */
  name?: string; // Optional, defaults to a generic placeholder if not specified
  
  /** Type descriptor indicating how the object is structured internally (e.g., 'struct', 'array', 'map'). Used by abstract_data_type_generator.js to infer underlying types. */
  type: "structure" | "collection"; 
}

/**
 * Interface for connecting to various doohickeys— no markdown fences, no commentary, no explanation.
 */

export interface DoohickeyTypeDescriptor {
  /** Unique identifier of the doohickey (e.g., 'dohkey_12345') */
  id: string;
  
  /** Name or alias for the doohickey instance */
  name?: string; // Optional, defaults to a generic placeholder if not specified
  
  /** Type descriptor indicating how the object is structured internally (e.g., 'struct', 'array', 'map'). Used by abstract_data_type_generator.js to infer underlying types. */
  type: "structure" | "collection"; 
}

/**
 * Interface for connecting to various doohickeys— no markdown fences, no commentary, no explanation.
 */

export interface DoohickeyConnectionDescriptor {
  /** Unique identifier of the doohickey (e.g., 'dohkey_12345') */
  id: string;
  
  /** Name or alias for the doohickey instance */
  name?: string; // Optional, defaults to a generic placeholder if not specified
  
  /** Type descriptor indicating how the object is structured internally (e.g., 'struct', 'array', 'map').
